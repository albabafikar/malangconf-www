# ----------------------------------------------------------------
# SECURITY ENGAGEMENT
# ----------------------------------------------------------------

# Does not allow access executable files
# Allow only if not an executable
# / static resource that allow to be access
# -------------------------------------------

<Files ~ "\.(?i:(?!(php(.+)?|inc|cgi(.+)?|s?p?s?html?s?|logs?|exec?|msi|sh(ell)?|s?bin|cmd|ini|p?env)))">
    Allow From All
</Files>
